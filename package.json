{
  "scripts": {
    "build": "babel --stage 0 query-autocomplete.js > build.js",
    "watch": "npm run build && chokidar query-autocomplete.js -c \"npm run build\""
  },
  "devDependencies": {
    "babel": "^5.4.7",
    "chokidar-cli": "^0.2.1"
  }
}
